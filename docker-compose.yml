version: '3'

services:
  interfacebuilder.webapi:
    image: interfacebuilder.webapi
    build:
      context: .
      dockerfile: InterfaceBuilder.WebApi/Dockerfile
    ports:
        - "50001:50001"
